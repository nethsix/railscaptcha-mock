#rake -T | grep reset_password for commands

namespace :reset_password do
  desc "TODO"
  task :reset => :environment do

  end

  desc "TODO"
  task :send_emails => :environment do
    #you will all need to reset your passwords email.
  end
end
